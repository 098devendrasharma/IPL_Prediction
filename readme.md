# IPL Score Prediction 🏏

A web application that predicts Indian Premier League (IPL) match outcomes (or first-inning scores) using a machine learning model.

This project uses Python, machine learning, and a simple web UI to take match inputs and produce a predicted score/outcome.

---

## 🔍 Project Overview

The IPL Prediction App is designed to predict cricket scores using historical IPL data and a trained model (`pipe.pkl`). Users interact with the app via a web interface (Flask) that takes match details and displays predicted outcomes.

---

## 📁 Repository Structure

/
├── Prediction/ # Prediction module with model code
├── templates/ # HTML templates for UI
├── app.py # Flask app entry point
├── pipe.pkl # Trained machine learning model
├── requirements.txt # Python dependencies
└── README.md 
